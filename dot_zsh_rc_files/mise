# mise — polyglot version manager (node, etc.). Replaces nvm.
# Hooks into the prompt to set tool versions per directory (.mise.toml / .tool-versions).
command -v mise >/dev/null 2>&1 && eval "$(mise activate zsh)"
