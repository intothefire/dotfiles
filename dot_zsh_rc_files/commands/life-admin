life() {
  command -v openclaw >/dev/null 2>&1 || { echo "openclaw not installed"; return 1; }
  openclaw tui \
    --url wss://trillian.tail8749b.ts.net \
    --token "${OPENCLAW_TOKEN:?set OPENCLAW_TOKEN in ~/.config/.secrets}"
}
