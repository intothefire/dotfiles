{{- /* Homebrew casks (macOS only), templated by role. Applied by run_once_01. */ -}}
{{- if eq .chezmoi.os "darwin" -}}
{{- $laptop := eq .role "laptop" -}}
# role = {{ .role }} — regenerated from Caskfile.tmpl.

# --- CLI tools + connectivity (laptop + mac-dev) ---
cask "1password-cli"       # provides `op`
cask "gcloud-cli"
cask "kiro-cli"
cask "antigravity-cli"
cask "tailscale-app"       # SSH over the tailnet

# --- Mobile / RN toolchain (laptop + mac-dev) ---
cask "zulu@17"             # Java for React Native / Gradle
{{ if $laptop }}
# ===== GUI apps — laptop only =====

# security
cask "1password"

# AI
cask "claude"
cask "chatgpt"
cask "codexbar"

# browsers
cask "google-chrome"
cask "chromium"
cask "firefox"

# cloud / sync
cask "dropbox"
cask "google-drive"

# productivity / PKM
cask "obsidian"
cask "raycast"
cask "setapp"
cask "hazel"
cask "hyperkey"

# comms
cask "slack"
cask "whatsapp"
cask "zoom"
cask "linear"

# dev
cask "iterm2"
cask "tower"
cask "kaleidoscope"
cask "antigravity-ide"

# media / design
cask "figma"
cask "vlc"
cask "handbrake-app"

# hardware / desk
cask "elgato-camera-hub"
cask "elgato-control-center"
cask "elgato-stream-deck"
cask "keyboard-cleaner"
cask "mutedeck"
{{ end -}}
{{ end -}}
